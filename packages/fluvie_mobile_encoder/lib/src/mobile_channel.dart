/// The platform-channel name the on-device encoder, video probe, and frame
/// extractor all speak over (methods `encode`, `probeVideo`, `extractFrames`).
const String mobileEncoderChannelName = 'dev.fluvie/mobile_encoder';
