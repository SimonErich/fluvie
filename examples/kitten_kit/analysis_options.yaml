# Shared support package for the example apps: root strictness + custom_lint so
# the Fluvie lint rules run over the composition builders too.
include: ../../analysis_options.yaml

analyzer:
  plugins:
    - custom_lint
