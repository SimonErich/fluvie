# slides

The deployable Fluvie presenter shell (slides.fluvie.dev). Open a local
`.fluvie` file in the browser, or run a bundled example presentation, and
present it with [`fluvie_presenter`](../../packages/fluvie_presenter).

```sh
flutter run -d chrome   # web (the primary path)
flutter run -d linux    # desktop
```
